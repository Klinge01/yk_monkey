---
// Redirect to default language
// For static builds, use Response.redirect() which works better than Astro.redirect()
// Use a dummy origin since we only need the pathname - Astro will handle the base path
const base = import.meta.env.BASE_URL || '/';
const normalizedBase = base.endsWith('/') ? base : base + '/';
return Response.redirect(new URL(`${normalizedBase}de/`, 'https://example.com'), 301);
---


